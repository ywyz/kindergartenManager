# Caddyfile — 反向代理配置
#
# 默认 Compose 仅支持配置了公网域名的 HTTPS 生产入口。
# CADDY_DOMAIN 必须在 .env 中显式设置为已解析到本机的域名。
# 本地开发请使用 docker-compose.dev.yml 直接访问应用端口，不启动 Caddy。
#
# Caddy 会自动为域名申请 Let's Encrypt 证书并续期。

https://{$CADDY_DOMAIN} {
	# 主系统反向代理
	reverse_proxy app:8080
}
