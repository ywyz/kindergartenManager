# 2026-09-15 文档与 Issue 审计

## 基线及证据范围

- 审计源码：`e1269d187017fabcf7bec0761f6a67023cb80437`（远端 main，beta14）。本地主工作树有未推送文档提交 `2f80467` 和三处未提交修改，均保留；本次使用独立 worktree。
- Main 实际核对生产容器 revision 与上述 SHA 一致，镜像为 `ghcr.io/ywyz/kindergartenmanager@sha256:60ed5b4aa146757e6dc0cdadf9875199763f5542bb9502763b8ae6c0067c9895`。HTTPS liveness 和 database readiness 成功；本次登录、bug 业务验收和新版本部署尚未完成。
- beta14 的 Quality `34906786865` 和 Release Build `34906786819` 已回读为该精确 SHA 的 success。这是既有版本证据，不替代后续修复的 CI。
- GitHub 全部 66 个 Issue（26 OPEN、40 CLOSED）的正文、评论、状态已取得；OpenCode 审阅并修改总览文档，pi 根据完整标题清单和开放需求正文独立分类。pi 未声称源码或线上复现。原始快照和工具日志保存在本机 `/tmp/km-audit-20260915/`，不是长期验收制品。

## 文档处理

- 纠正 `CONTEXT.md`、`README.md`、`docs/ROADMAP.md` 的旧发布/迁移 head/未实施描述。当前 head 为 `a608bc23457b`；以迁移文件为权威。
- 周计划 README、current-status 和两份架构/数据模型文档增加当前入口，明确旧 Windows 交接、旧显式取源流程与旧阶段限制的历史时点。
- 删除总览中把 beta9、旧迁移 head 和未落地阶段当作当前事实的表述。没有发现可以安全整份删除且不损害追溯的历史证据文件，因此不删除历史账本、验证样本或交接材料。
- 新增本审计记录，集中说明问题清单、需求歧义和未完成事项。旧发布的登录、Word、MySQL、回滚结果不改写为本次 PASS。

## Bug 清单

| Issue | 审计时事实 | 后续动作 |
|---|---|---|
| [#71](https://github.com/ywyz/kindergartenManager/issues/71) | 当前合成教师首页 5 项，侧边栏 9 项；遗漏倾听、班级配置、提示词、个人资料。源码有独立硬编码列表；本地浏览器已确认 | 使用现有按角色导航投影，验证首页/侧边栏一致及窄屏 |
| [#72](https://github.com/ywyz/kindergartenManager/issues/72) | 本地真实浏览器：home 选夜间→每日计划→未选日期点保存；失败提示后 body 为 body--light，但 theme=night、夜间 aria-pressed=true | 修复组件状态同步，保持保存/权限逻辑；新代码另验 |
| [#80](https://github.com/ywyz/kindergartenManager/issues/80) | 已定位 agent.tool_schema_invalid 的“关闭契约”文案；用户具体触发路径和底层原因仍未知 | 保持四 READ + 两 DRAFT 与严格校验，复现并提供安全诊断 |
| [#81](https://github.com/ywyz/kindergartenManager/issues/81) | 已取得上传及生成回调；“已上传”的具体状态/按钮仍待确认。浏览器上传受扩展权限限制，未进入应用 | 用合成图片和异步状态回归定位，不推断存储根因 |

已关闭 #23、#24、#61 为历史缺陷，不能因本次盘点重新宣称修复。#54、#58 含运维/契约实现与独立验收门；保持其原门和未完成状态。其余开放 Issue 为功能、平台或阶段跟踪项，不把整个需求实现伪装成 bug 修复。

## 需求歧义

已向用户提问，未擅自冻结答案：

1. #80 的具体操作与完整提示；#81 是否显示预览、哪个按钮阻挡及提示原文。
2. #78 同一人兼任管理员和教师时，是两个账号还是同账号切换角色。
3. #79 “恢复默认”指园所当前全局提示词还是系统出厂提示词。

其他后续需求尚需各自设计冻结：#70 联网开关对应能力边界；#63 注册后启用/审批策略；#65 旧库版本范围；#68 相似度阈值与导入格式；#59 对象存储选型。它们不阻塞四项 bug 的独立排查，不在本轮扩展实现。

## Issue 回写

已更新 #71、#72 的当前数量与修复前复现证据，以及 #75、#77 的现行周计划范围入口。保留原始正文和历史评论；不删除 Issue、不提前关闭验收勾选。

## 提交与验证边界

本次文档提交仅包含 Markdown。Main 检查 diff、改动范围和新增本地链接，不为文案编写测试。后续 bug 代码、测试、CI、Release、生产部署及登录/业务验收另行记录，不在本文件预写完成。
