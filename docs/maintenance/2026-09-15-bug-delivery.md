# 2026-09-15 Bug 修复交付记录

## 已完成的文档阶段

独立 docs-only 提交：`3f80004054f4212e08b8eeded219ba3658ecd70c`。仅 8 个 Markdown 文件；新增 13 个本地链接均有效，`git diff --check` 通过。Issue #54、#58、#71、#72、#75、#77 已补充当前状态或历史范围说明，保留原始正文与所有评论。

扫描 docs/ 与 specs/ 的 Markdown 相对链接：46 处失效链接均位于 prior-* 历史原件副本；当前文档未发现缺失相对目标。为保持归档字节/hash，不改写这些原件副本。

## 代码与本地证据

| Issue | 本轮处理 | 验证状态 |
|---|---|---|
| #71 | 首页/侧边栏统一导航投影 | 本地浏览器 RED→GREEN：教师首页由 5 项补齐至侧边栏同样的 9 项；390px 窄屏无横向溢出 |
| #72 | 夜间偏好与组件状态同步 | 本地浏览器 RED→GREEN：校验失败、实际草稿保存成功均保持夜间；跨标签页切换日间同步通过 |
| #80 | 固定分类错误码与可理解反馈；未知错误不回显 | 新增 7 项反馈测试 RED→GREEN；既有 UI 集成 19 项、零持久化 38 项通过。线上原始根因未确认，Issue 保持 OPEN |
| #81 | 上传处理中明确提示；读取失败原子丢弃本批次并提示重传；冻结点击时状态，禁止回调覆盖新上传 | 处理竞态与读取异常在旧代码 RED；新回归和既有会话保护共 103 项通过。真实浏览器上传及线上用户场景仍未验证 |

OpenCode 实现导航与主题，pi 审查 issue 并提供上传初稿；连接失败后仅移除各自进程的大小写代理变量，后续调用成功，未修改全局代理。Main 拒绝 pi 初稿中等待上传后重新绑定输入及 action guard 释放不完整的方案；纠正调用超时后由 Main 完成安全修正与独立验证。OpenCode 输出代码和测试后最终回复停滞，Main 停止该进程并独立检查产物；不把进程超时当作成功认证。

本地检查：导航/菜单/主题 53 项通过；随后窄屏标题及异常范围微调对应主题 31 项通过；Agent 新反馈 7 项、既有 UI 19 项、零持久化 38 项通过；上传及业务页面会话保护 103 项通过。以上有重叠，不相加为唯一测试数。9 个变更 Python 文件 Ruff check、format --check 与 git diff --check 通过。没有运行本地全套测试或真实模型验收。

本地使用 `/home/ywyz/km-bug-venv-20260915/bin/python`（Python 3.14.7），共享主环境已安装依赖并在新 venv 补齐仓库锁定的 chinesecalendar 1.11.0。复现数据库为 `/home/ywyz/km-bug-fixture-ready-20260915/`，只含合成数据。零持久化测试初次因进程设置 KINDERGARTEN_DATA_DIR 与 fixture 要求冲突而 setup 失败；取消该变量后失败组 38 项通过，未更改产品逻辑或放松测试。

## 交付门与未解决事项

- 修复代码提交 `3cf234ec251faebe75bb99096f92bfbcb954a9bd`；远程 CI 结果待回填，Release、生产部署与修复后业务验收尚未执行，不沿用 beta14 结果。
- 生产浏览器无可用登录会话；历史合成口令已被拒绝，已请求用户登录或提供受保护凭据路径，未重置密码。
- Edge 扩展 file chooser 设置文件返回 Not allowed；已说明需启用 Allow access to file URLs，合成图片尚未由浏览器上传。
- #80 的实际操作/完整提示与 #81 的预览/触发按钮仍待用户补充。
- 不修改真实教师正文、不增加 Agent 能力、不重构。生产切换仍需不可变镜像、有效备份恢复证据及独立登录/业务门；候选通过前不切流量。

## 远程检查发现的文档遗漏

`198e8bb8ed775d87d131e6e6d45fcb4caf3fe9c6` 的 Quality push run `34937578934`：1902 passed、1 skipped、21 deselected，唯一失败是既有文档测试硬编码旧 head `3c9f4b2a7d1e`；未进入 Foundation 步骤。真实 LibreOffice job 通过，CodeQL 通过。将该既有检查改为读取 Alembic 实际唯一 head 后，同时更正开发指南、手工检查入口、ADR 当前指针与历史索引的遗漏。保留旧 revision 的历史含义，不将旧验收升级为新结果。本地该文档契约组 16 项通过。

候选镜像 run `34937701417` 首次因 Debian ARM64 包下载连接中断而失败，已对同 SHA 重试；尚无可部署结果，不修改依赖快照或降低构建门。后续新 SHA 的 CI 和 Release 仍需单独核对。
