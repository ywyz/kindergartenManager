# Issue 77 周计划 WP-A 执行提示词

> 下方保留2026-09-08原始提示词。2026-09-09用户批准的阶段调整优先：依赖新共享接口的可执行业务RED移至WP-C首门；WP-A冻结契约与断言、保留当前差距RED。排版证据门不变。执行时以当前spec/tasks为准。

```text
请推进 kindergartenManager Issue #77 的 WP-A：实现基线与契约设计。
必须调用子代理，完成后继 ADR、迁移提案、五/六列单页排版验证、首批稳定 RED 和独立复审。
本轮不实施 WP-B～WP-F 的产品 GREEN，不改月计划，不部署或操作真实业务数据库。

一、读取权威需求与现状

完整读取 AGENTS.md、specs/weekly-plan-authoring/spec.md、tasks.md、CONTEXT.md、docs/ROADMAP.md，
以及相关 ADR、数据模型、架构设计。实时读取以下 Issue 的正文与相关最新评论：
- #77：https://github.com/ywyz/kindergartenManager/issues/77，本次功能范围和验收标准。
- #57：https://github.com/ywyz/kindergartenManager/issues/57，周/月总项与历史 WMP 证据。
- #75：https://github.com/ywyz/kindergartenManager/issues/75，权威班级身份与成员依赖。
- #55：https://github.com/ywyz/kindergartenManager/issues/55，平台总项。
- #56：https://github.com/ywyz/kindergartenManager/issues/56，模板资格和绑定的相关证据。

#57 原正文的“当前唯一授权：WMP-3”已标为历史范围，不得用它阻止 #77 当前任务。
#75 的非创建者默认不可编辑规则有明确的新周计划例外，不适用于其他业务共同编辑。
不要重复询问已确认的产品规则，也不要把 Issue 创建、设计记录或历史 GREEN 当作实现/验收通过。

二、Main 建立实现基线

实时核对本地 HEAD、origin/main、迁移 head 及已有周计划导出修复，记录实际工作 SHA。
保护用户所有未提交修改，包括 graphify-out/cache/last_query_stamp 和新规格文档。
建立独立 worktree，安全复制并核对本次已确认的未提交规格，不丢弃原工作区修改。
本地与远端不同不是云端部署错误；本轮不是旧 WMP-9 正式验收，不盲用旧固定基线门。
规格目前可能未提交，不能假设 Issue 中的本地路径是可访问的 GitHub 文件链接。

三、明确调用子代理

使用当前配置模型，不自行指定模型。子代理不递归委派。
对所有子代理说明：你不是独自在代码库工作，不得撤销他人修改，必须遵守写入范围。
Main 独占 ADR/spec/tasks/路线图/架构文档和测试代码的写入；前三个子代理负责只读调研，
只有模板子代理可在其专属临时目录写合成样本、脚本与渲染证据。彼此不重复广泛扫描。

子代理 A：班级身份与共享授权
- 只读研究 #75、当前用户/学期/班级/每日计划/周计划模型和授权路径。
- 给出 #77 必需的最小身份子集，与 #75 完整学年历史、升班等范围分开。
- 定义同班同周唯一键、有效成员、撤销、实际编辑者、不可变版本、CAS、窄字段投影与审计。
- 同日重复检测包含本人+他人，不允许名称授权；周计划共享不得扩大源每日计划写权限。
- 输出后继 ADR 要取代 ADR-0009 的精确条款、迁移建议与稳定行为测试清单。

子代理 B：每日活动名称和来源快照
- 只读追踪拆分 prompt→结构化 schema→服务→UI→保存/重载→API/Word 的调用路径。
- 检查历史空名称、旧自定义提示词、revision、产品 Agent 字段白名单兼容。
- 设计来源候选选择、源 revision/字段关系、变化提醒、重新导入差异及不反写源记录的契约。
- 输出精确文件/符号清单、兼容风险和有意义的回归用例，不修改产品代码。

子代理 C：单页 Word 排版可行性
- 使用 Documents 技能检查实际 weekplan.docx，阅读相关已修复 renderer 和模板资格规则。
- 只在独占临时目录生成正常五天、前置周日六天、周六六天合成样本；覆盖最短完整及长中文样本。
- 保持所有固定游戏/目标/栏目数量、A4、宋体小四、固定20磅行距，实际渲染并检查页数、裁切与合并。
- 不覆盖仓库模板，不更改固定 hash/profile，不将填充样本标为正式导出或资格通过。
- 给出可复现命令、产物 hash/页数/环境和截图，以及长度预算和缩减候选确认方案。
- 缺字体/渲染器就报告具体缺项；不得以替代字体、字符估算或单一客户端宣称双平台 PASS。

Main 同时：
- 梳理实际学期、教学周唯一身份、前置周日归属、五/六列、假期空格、整周休假计数与跨年规则。
- 说明日历不支持年份、七列组合的处理；不静默丢日或让同一日期属于两份周计划。
- 合并依赖图并区分用户已确认规则、待评估技术方案与真正的产品歧义。

四、集成方案并建立首批 RED

1. 编写限定于新周计划的后继 ADR：同班共享、草稿导出无审核、人员默认与快照分离、
   成员撤销、来源快照、AI候选确认和月计划/旧五天记录兼容。
2. 给出 Alembic 提案：表/字段、唯一约束、tenant复合外键、索引、旧数据显式映射、
   MySQL/SQLite兼容、回退限制与数据保留；不执行真实迁移。
3. AI使用应用按钮触发的窄服务及现有适配器，不增加产品Agent工具或Provider WRITE。
   缩减可覆盖手工/导入文字，但必须先展示差异再确认；正式exporter不自动调用AI。
4. 固定首批行为 RED：共享唯一性、跨班拒绝、成员撤销、重复备课人工选择、源变化不覆盖、
   日期唯一归属和固定内容数量。不得用 assert False、文件存在或偶然环境错误冒充业务 RED。
5. 在隔离环境实际执行并记录每项失败原因；不得为了GREEN提前实现产品。
   已存在的相关通过测试可以作为基线，但不能称为新功能通过。
6. 更新 spec/tasks 和必要的架构/路线图指针，所有未实现项保持准确状态。

五、调用 reviewer 子代理独立复审

前三个子代理完成后，调用 reviewer 进行只读复审，不让其修改作者文件：
- #77 的用户规则是否完整，是否未经确认改变数量、权限、共享或排版要求；
- 与 #75/#57/#56 的依赖和历史兼容是否明确，是否不必要地扩大范围；
- 租户/成员/并发/迁移方案是否存在漏洞；
- 排版证据是否能支持后续实现，RED是否验证真实行为且稳定。
Main修正设计和测试后按需复审；存在finding就如实报告，不虚构Review 0/0。

六、交付和 Issue 回写

在 #77 回写本门脱敏结果：工作SHA、规格来源、子代理结论、文档/测试清单、排版证据、
RED命令及预期失败原因、Review finding和剩余缺项。仅勾选实际完成的检查项。
如出现需要 #75 或 #56 协调的具体依赖，添加精确交叉引用；#55/#57仅保留简短进度索引，
不要复制多份完整规格或改写历史tested-code/closure/CI证据。
未提交文件只列本地路径；若后续已有可访问提交，再补固定SHA链接，不伪造链接或CI。
不关闭 #77/#57/#75，不创建PR、不发布、不部署、不执行生产迁移。

最终给用户：WP-A是否完成、证据与限制、尚需决定的最小问题，以及WP-B下一门精确范围。
持续完成已授权且可独立推进的工作；不得因某个Office环境缺失而停掉其余设计/RED工作，
也不得在缺少必要证据时把本门全部标成PASS。
```
