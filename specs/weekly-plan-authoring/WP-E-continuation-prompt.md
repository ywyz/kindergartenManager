# WP-E 续作与阻塞提示词（不是 WP-F 授权）

继续 Issue #77 的 WP-E。先读取最新用户授权、AGENTS、ADR-0011、spec §2.2–2.7、tasks WP-E/F、本轮 `WP-E-local-contract.md`、交付清单及跨模块独立复审；现场核验最终tested_code_sha、文档后继、全部材料hash、工作区状态、唯一Alembic head。不要从本文推断远端状态或授权。

2026-09-12用户明确允许四类WP-C历史native双RED缺口保留UNMET而继续WP-E本地实现/隔离验证；四类缺口没有补写成PASS，原177/178 raw hash与JSON重写时序解释保留。WP-D关闭证据及前置核查继续保留，不能把这次授权自动扩大为WP-F、远端或发布授权。

保全既有worktree、暂存/未跟踪材料和外部证据；新建隔离worktree，必要输入逐件核对hash。先确认已有本地填写、来源、生成、保存、检查、缩减、导出链和本轮修复均仍成立。新行为缺口先保存修复前源tar/hash，再连续两次真实应用业务RED、最小GREEN和独立只读复审；环境错误、缺接口或测试编写错误不计RED。既有初次覆盖也不改成历史RED。

剩余重点：

1. 取得当前正式受控模板与本代码五/六列独立Microsoft Word资格证据：记录具体Word产品/版本，正常/长中文、多姓名、空格、书名号、数量、日期、假期、换行、全部页原生预览、无裁切/字体漂移。long三份两页FAIL与compact仅合成候选历史不变。不能以LibreOffice、OOXML、PDF页数或文档清单替代Word。
2. 按可信catalog契约审阅并安装资格，绑定原模板hash、当前released UUID/version/contract、实际renderer版本与五/六列材料hash。默认无资格失败关闭不是正式导出PASS。本地synthetic catalog不得移作生产资格。
3. 对最终tested_code_sha补齐受控目标页面全分支：重复源零/一/多、缺项生成、选字段重生成、拒绝/采用、两教师共享人员不覆盖、取消/候选过期、所有漂移、保存/对账、真实超页→有限缩减→明确采用/保存→再检测/手改。已有localhost操作只覆盖其记录的步骤，不能外推云端。
4. exact-SHA远端CI、不可变OCI镜像、数据库revision、受控云端浏览器、真实模型及凭据/真实迁移分别检查明确授权。未授权项保持BLOCKED，不执行，不从本地提交推断远端。真实模型只走操作教师应用配置/解密，不读出或注入真实key。
5. 若本地代码再改，重新冻结代码SHA，运行适用SQLite每连接FK/专属MySQL迁移、并发/两种撤销线性化、取消/replay/commit_unknown只读对账、旧版本/hash/源/人员不变，常规/Foundation/旧周月/Agent回归及lint/format/diff。独立复审不得审自己实现，Main合并结论；保留业务RED与初次覆盖角色。

任一必需门未满足仍不写WP-E COMPLETE。只有实际关闭且用户明确授权Issue消息才回写#77并逐字读回；不关闭#77/#57/#75。push/PR/合并/部署/生产发布分别授权；不实现月计划/cohort/#78/#79或扩展产品Agent。

仅当WP-E实际完整关闭后，再按tasks WP-F全矩阵写 `WP-F-next-prompt.md`，承接每日名称、身份/共享/人员/来源、日历/AI/缩减/导出、旧兼容、隔离持久化与Word全分支及未完成外部门。此前继续使用本WP-E续作提示词，不执行WP-F。
