# WP-E 执行提示词（待另行授权）

继续 Issue #77 的 WP-E「填写页面与单页导出」。本文件本身不授权执行。先读取最新用户授权、AGENTS.md、ADR-0011、spec §2.2–2.7、tasks WP-E/F、当前状态、WP-D关闭矩阵/最终证据清单及独立复审。实时核对WP-D tested_code_sha `258a9b0978e89397c90a4b7e271388d58b13b8d7`、文档后继、干净状态、唯一Alembic head和材料hash；以实际关闭证据判断可否继续，不直接沿用本提示词中的SHA。WP-D若有必需门未满足，先处理该门，不进入完整WP-E实现。

WP-C四类修复前native双RED历史缺口继续未满足；2026-09-11用户仅允许哈希差异查明后继续WP-D依赖，不把此处置自动扩大为其他阶段的豁免。原WP-C清单177/178 raw hash事实与JSON重写时序解释均保留。下一阶段依赖处置按用户最新授权核对，不补写历史RED。

保全全部既有工作区、暂存/未跟踪材料和证据，新建隔离worktree，只复制必要输入并核对hash。只做授权范围内的本地代码、文档和隔离验证；push、PR、合并、Issue消息、部署、真实模型、真实迁移、生产发布分别核对明确授权。不得关闭#77/#57/#75或顺带实现月计划、cohort、账户职责#78、全局提示词#79。

先冻结“需求→真实UI/应用入口→输入输出/拒绝→测试→证据→状态”矩阵、模板资格及导出契约，再按依赖完整完成以下工作，不能一个子步GREEN就结束整门：

1. 替换周计划教师入口为完整填写/生成/保存/导出流程，沿用每日页面交互风格。连接现有 `composition.authoring`，不再建授权policy或共享根。展示固定标题、规范日期五/六列、学期周次、授权班级、主题书名号和共享人员；多教师打开不覆盖已存人员/正文。显示同班共享、最后编辑者、重复来源选择、来源变化和未保存修改。
2. 以 `weekly-authoring.v3` 的固定slot/schema/calendar/budget/archive为正文权威，完整提供手填、显式来源选取、缺项生成、选定字段重生成、原文/候选差异、拒绝/采用和双CAS保存/重载。独立activity_name缺失待手填，假期格不生成。确认只发送opaque ID/关闭协议，候选过期、取消、撤销、源/mapping/page/prompt/target漂移均保留正文；普通手改历史与本次live来源重验边界保留。
3. 重新实施五/六列真实模板资格、填充与回读，绑定当前正式模板/active版本。固定A4、宋体12pt（小四）、固定20pt行距、栏目/数量/日期顺序/假期标签/多姓名/换行，无样例残留。历史WP-A compact试排只是合成可行性候选，不能成为正式模板资格或产品通过。
4. 保存后的不可变版本才可进入单页检测；缺必需项、schema错误、缺字体/渲染器、超页/裁切/溢出均明确失败，不返回已知超页文件。不把字数预算或OOXML结构估算当成实际一页。保留合法缺项草稿保存，不用导出完整性阻止保存。
5. 在现有提示词管理中登记篇幅缩减任务及固定关闭schema。建立有限次数（按ADR最多两轮候选）的渲染→缩减差异→显式采用→重新检查流程；不自动重试，不删除事实/标题/日期/数量/假期格，不暗改字号行距。手工、导入和AI值的修改均展示差异并确认；拒绝不改内存或版本。复用操作教师支持的配置/解密和integration，无Provider长期对话/候选、无产品Agent新增工具或WRITE。
6. 正式exporter仅消费明确保存版本，不能内部自动AI或隐式保存。进入、等待后、最终交付重验当前actor/session/assignment、plan双CAS、模板binding及适用来源/候选基线；撤销或版本变化不交付。遵守旧导出契约的临时DOCX/PDF/PNG清理、无新增ExportRecord/持久预览，不反写DailyPlan。无审核作为前置，不扩产品审核流程。

对新发现行为缺口保存修复前精确源tar/hash、连续两次真实应用业务RED，再最小GREEN及独立只读复审。接口缺失、NameError、环境/字体缺失或文件存在断言不能冒充业务RED；初次通过单列覆盖。可委派有边界非重叠任务，不递归，Main负责集成和整门结论。

在一次性SQLite（每连接FK）和专属MySQL验证适用迁移、并发、撤销两种线性化顺序、取消/重放/commit_unknown只读对账、无半提交、旧版本/hash/来源/人员不变；运行适用常规、Foundation、旧周/月、Agent回归与lint/format/diff。受控UI须真正读取和操作目标页面，不能用浏览器发现/导航或helper测试代替。云端测试站、真实模型和凭据操作需单独授权；无授权则该外部门保留BLOCKED，不用localhost结果写云端PASS。

Word为主要格式客户端，LibreOffice仅备用打开；实测记录具体Microsoft Word产品/版本、全部页原生预览、裁切/字体/固定数量。long三份两页FAIL和compact仅合成候选的历史事实不改写。缺实际Word证据不推断PASS；文档完整性检查不等于Office验收。

交付须绑定最终tested_code_sha、文档后继SHA、Main/Reviewer、两库/模拟AI/远端CI/真实模型/云端/Word的证据角色及可校验清单。任何必需项未满足，不写WP-E COMPLETE。只有用户明确授权消息且实际满足本门时才回写#77并逐字读回，远端状态不由本地提交推断。

WP-E实际关闭后，再按tasks WP-F全矩阵和未完成外部门撰写 `WP-F-next-prompt.md`：承接每日名称、身份/共享/人员/来源、日历/AI/缩减/导出、旧兼容、隔离持久化与Word全分支，绑定exact-SHA CI、不可变OCI镜像、数据库revision、受控云端浏览器和实测Word版本。测试站部署、真实模型、凭据/真实迁移与生产发布分别核对授权。不得跳过WP-E直接执行WP-F；WP-E未完整时只写续作/阻塞提示词。
