# WP-A 实现基线与设计证据

2026-09-08；本轮设计/诊断交付已回写，WP-A 总门BLOCKED、尚未完成。产品 WP-B～WP-F 均未实现。

## 权威来源与工作区

实时通过 `gh issue view N --json number,title,body,comments` 读取 #77/#75/#57/#55/#56 全正文及评论，
本地原始快照在 `/tmp/km-issueN.json`。#77 是新功能权威，#57“唯一授权WMP-3”已明确历史化，
#75 的非创建者默认拒绝对新周计划有显式例外，不扩展其它业务。

- 原工作区 `/home/ywyz/code/kindergartenManager`，HEAD `01bf6349a7c6f5268eba4425212f546874d651b0`。
- `git fetch origin main` 后 `origin/main=8dcc83376577695b562529ec42adc6b48817f004`。
- 独立 detached worktree `/home/ywyz/code/km-wpa-20260908` 使用该远端 SHA，未提交本门文件，未创建 PR。
- HEAD 到远端的产品差异为 `bd2457a` 的 merged game rows 全日期内容保留修复；另两笔是文档修复/证据。
  本轮按当前源码复测，不使用旧固定 WMP-9 起点门，也不据本地/远端差异判断云端部署故障。
- `python -m alembic heads` 只读源码迁移图：唯一 `3c9f4b2a7d1e`，未连接真实业务数据库。
- 用户6份文档和Graphify缓存的完整patch经 `git apply --3way` 复制至新基线；未提交spec目录逐文件复制并核对
  SHA-256。原输入hash见 `input-hashes.json`，备份patch在 `/tmp/km-wpa-20260908/user.patch`。
  未复制 `.env`、数据库、真实Key或secret文件；原工作区没有reset/stash/覆盖。
- 规格原始hash：spec `5f2ad97a6aad87a005a091d3e609a305da436d49431b3383c1d80dd404c5b4f6`；
  tasks `d4d1db95204130dd5602dd5a2aa6db461f262923fe3ca3406559ba23a5282d93`；
  WP-A prompt `545d9bdfea0cf2070a027a07102cc2b23b07fe62b9525c44e0380a8424e0c7e3`。

这些未提交路径不能作为 GitHub blob 链接；没有新 tested-code commit、closure commit 或本门 CI。

## 文档与依赖

- `docs/ADR/ADR-0011-shared-weekly-authoring-and-source-snapshots.md`：仅新周计划取代条款。
- `specs/weekly-plan-authoring/migration-proposal.md`：分门Alembic提案，无 migration Python。
- `specs/weekly-plan-authoring/calendar-contract.md`：实际学期、锚点、前置周日、五/六列和异常拒绝。
- `specs/weekly-plan-authoring/service-contract.md`：目标窄服务入口与尚缺的行为测试，未预建产品空壳。
- spec/tasks/架构与路线图仅更新指针和实际状态，历史 tested-code/closure/CI 原样保留。

依赖链：WP-B名称 → WP-C最小#75 identity/member+共享/来源 → WP-D日历/固定结构/AI →
WP-E五/六列新模板#56资格+填写/保存/单页 → WP-F云端/Office独立验收。
WP-A试排与前几门可以并行取证；排版缺项不阻断每日名称设计，但阻止单页总门PASS。

## 子代理

初次 identity/daily_source/layout 三个调用均在运行前因默认子代理模型额度耗尽失败，无调研产物。
用户随后明确允许其它已配置角色；已用 `luna_worker` 角色重新调用三个独立子任务，未手动指定model。
三者均获只读/不递归/不撤销他人修改约束；仅C可写 `/tmp/km-wpa-layout-20260908`。
最终结论及独立review后续在本账本追加，不以调用成功推断调研完成。

## 首批当前行为差距 RED

命令（从独立worktree运行）：

```bash
/home/ywyz/code/kindergartenManager/.venv/bin/python specs/weekly-plan-authoring/evidence/run_red.py
```

runner每次创建owner-only临时设置根，显式内存SQLite，退出清理；测试使用真实当前ORM/repository/policy/renderer，
只seed合成数据，不启动UI、不调用真实网络/AI、不操作业务库。schema来自metadata，**不是Alembic验证**。
双次pytest均 exit1：**16 collected / 12 failed / 4 passed**，节点和结果一致。
node-only SHA-256 `4e27e606dfa7cb334a3c850f1d9080819e33c5d8fb132e7b8522ea7eec830274`。
日志 `red-run-1.log`/`red-run-2.log`，完整节点 `red-nodes-1.txt`/`red-nodes-2.txt`，测试内容hash见 `red-results.json`。
runner自身exit0只表示稳定分布匹配，不表示产品GREEN。

| 行为 | 实测 | 证据含义 |
|---|---|---|
| 同班同周两个个人root | 两份而非一份，RED1 | 旧存储无法提供共享唯一性；无assignment/semester，非新共享并发验收 |
| 本人+另一教师候选 | owner list仅1条，RED1 | 不能重用个人查询实现重复人工选择；不得扩大该函数/按同名授权 |
| activity_name拆分保留与保存重载 | 明确名称被过滤/保存白名单拒绝，RED2 | WP-B真实链路缺口，未改变产品代码 |
| 前置周日周次 | 两日期分属相邻周，RED2 | 新教学锚点需单独处理，不改其它模块自然周语义 |
| 第六日期 | 旧WeeklyDay明确拒绝周日/周六，RED2 | 版本化五/六列契约尚缺，不放宽旧DTO |
| 固定三项总结 | 两项文本仍被接受，RED3 | 新结构validator需有真实数量约束 |
| 整周固定游戏 | 同一组被按五日重复，RED1 | bd2457a保全每日内容正确；新整周汇总需独立映射 |
| 跨class/tenant、旧grant撤销 | baseline通过2项 | 已有策略证据，**不是新成员资格/撤销GREEN** |
| 源修改后detached周正文不变、跨user源读取拒绝 | baseline通过2项 | 基础隔离/内存快照，**不是来源变化提醒或重新导入验收** |

这些是当前真实行为的差距探针，不是要求把旧契约改成新契约。禁止为全GREEN破坏旧权限、数量、月计划或
bd2457a修复。尚缺新成员撤销/重复选择交互/来源检查与重新导入的可调用生产行为；不能用missing module、
assert False、文件存在、测试内假产品服务冒充这些业务RED。独立复审确认首批RED完整度不足，WP-A保持未完成。

## 已有基线复测

独立临时设置根中运行：

```bash
python -m pytest tests/test_released_weekly_plan_game_completeness.py \
  specs/weekly-monthly-plans/tests/test_weekly_monthly_domain_contracts_red.py -q -p no:cacheprovider
```

结果 `85 passed`，日志 `baseline-tests.log`。只证明旧domain和已修复renderer当前仍通过，不称新功能通过。
未运行全库/云端/生产MySQL/正式Office；本轮仅文档和测试，不以历史CI补证。

## A/B调研收敛

A已完成只读身份研究，图索引HEAD01bf634与目标8dcc833不同，只用图导航，目标源码为最终依据。
最小identity为year/semester/class_instance/class_semester/assignment/daily映射；完整#75升班/历史后置。
已采纳版本保存assignment/成员revision/operation、源日期绑定与同根日期唯一约束。表中管理资格不能自授。
A确认现有assignment生产seam缺失，新成员撤销仍BLOCKED；旧grant测试不是替代。

B已完成只读名称链路，定位split客户端只返回5字段、LessonPlanResult/模型/UI/API/Word均无名称。
已增加两个真实调用RED：显式名称被split过滤、保存名称被repository白名单拒绝。旧自定义五字段prompt应兼容
空名称并提示，不能覆盖用户prompt或编造名称。WP-B范围见下一门提示词。
B建议保持Agent写白名单，Main补充：完整操作前snapshot应显式版本化，保留旧v1对账、新v2包含名称，
不得因保持写白名单而丢完整业务快照。重复选择/check/reimport没有生产seam，不能声称完整行为RED。

A/B未写文件、未递归委派。C完成后已调用reviewer只读复审。

## C排版诊断（不构成宋体或双平台PASS）

C已完成，只有专属临时目录写入。实际仓库weekplan含两套9×7表格，直接渲染2页；候选仅在临时副本
移除第二套以验证单周页面，新结构必须在WP-E重新资格化，不能套用旧profile/hash/binding。

| 布局 | 最短完整内容（表格非空白字符数） | 长中文 | 额外缩减候选 |
|---|---|---|---|
| 常规五列 | 362；1页 | 1407；2页 | 491；1页 |
| 前置周日六列 | 378；1页 | 1488；2页 | 523；2页，家园共育续页 |
| 周六六列 | 378；1页 | 1488；2页 | 523；2页，家园共育续页 |

全部保留2集体+1自主各3目标、区域3目标/3指导、全部栏目和3名教师，OOXML正文12pt/exact20pt。
字符数只是这些特定合成样本的实测描述，不是产品长度阈值。六列523字仍2页，证明不能用总字符数判单页。
缩减设计采用逐字段候选、原文差异、明确采用、再实际渲染；禁暗删数量/事实/标签或降字号/行距/边距。

环境：bundled Python 3.12.14，Documents `render_docx.py`，bundled LibreOfficeDev
`26.8.0.0.alpha0 2c87e51eeaa2b413ff4ae097b2705eea1995d8e5`；缺load_workspace_dependencies工具时显式定位
`/home/ywyz/.cache/codex-runtimes/codex-primary-runtime/dependencies/`，没有使用用户桌面LibreOffice。
宿主Ubuntu26.04.1、Linux7.0.0-31-generic、x86_64；LANG=zh_CN.UTF-8、LC_CTYPE=C.UTF-8。
`fc-match 宋体/SimSun`和PDF嵌入字体均为Noto Serif CJK SC；**没有真实宋体字体证据**。
Windows Word缺失；LibreOffice为alpha构建，不能据版本数视为目标稳定客户端验收。

证据/脚本/最终PDF/截图在 `/tmp/km-wpa-layout-20260908/`：`artifact.md`、`evidence-manifest.json`、
`make_layout_samples.py`、`make_compact_candidates.py`、六个`render-final-*`及三个compact目录。
早期`render-*`基础目录包含已修正宽度问题，**不作为最终截图**。Main复查发现manifest误指早期目录，
已要求C按最终DOCX→最终PDF/PNG重新绑定hash；引用以修正后manifest为准。

首轮发现源五列grid总11052twips、早期六列10475twips均超可用10466twips，六列新增列边框不完整。
撤回早期全部无裁切的表述；早期同名产物已被新产物替代，须按最新hash识别，不能混用。
C修复后五列grid `[933,920,1722,1722,1723,1723,1723]`，六列
`[933,920,1435,1435,1435,1436,1436,1436]`，所有9份tblW与grid均为10466。
新增列在合并前复制边框，六个base与三个compact均重新渲染，页数仍如上表。
Main检查最新前置周日short：全部日期、右边框和底行在页内，未见横向裁切；仅该Linux替代字体的诊断。
已将C文本证据复制为本目录 `layout-artifact.md` 与 `layout-manifest.json`；DOCX/PDF/PNG/脚本仍在专属临时目录。
最新manifest SHA-256 `6c1b1ca0a829ab990a4a9086563e5bbbe7be0ccf308be6afc0eb31bc05738b39`，
artifact `40d490dca9e0cef2d5c49097b78e775b91fb9a48c6c9eeae2083c60404cd6721`。
真实宋体/Word、长文收敛与正式新profile资格仍BLOCKED，不能把页数诊断升级为排版通过。
本轮样本不是正式业务导出，不新增ExportRecord，不改受控仓库模板。

## 范围与保全检查

Python 3.14.7、Ruff 0.16.6；本门测试/runner的Ruff与format通过，新文档本地链接均可解析，diff whitespace检查通过。
原工作区全部输入hash复核仍一致，Graphify缓存也保留原值。独立worktree的app/templates/alembic/requirements
相对工作SHA无差异。模板仍为weekly `f6c17c137f04e29a68524ed400eb395984e93a16c234a065b5794d9f49a9347b`、
monthly `f2e5dbe2a468dd15c55cdd6b70c5e15fe63048a3708b151732e208703b0d11f4`。

## 独立复审

首轮 H/M/L=2/2/0；详见[逐项记录](review.md)。来源映射基线已补充并独立复核关闭R3，部分周成员权限待用户答复，
排版样本几何已修复并独立复核关闭R2；必需新共享行为RED仍缺。WP-A总门BLOCKED，不将调研/设计完成当实现完成。

最终Review剩余H/M/L=1/1/0：完整业务RED与部分周成员规则未闭合。宋体/Word、长文单页和正式资格均未通过。

## Issue回写及最终核对

已发布并读回正文、核对全部仍OPEN：
- [#77完整本门结果](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5586488373)
- [#75精确身份依赖](https://github.com/ywyz/kindergartenManager/issues/75#issuecomment-5586496162)
- [#56新模板资格依赖](https://github.com/ywyz/kindergartenManager/issues/56#issuecomment-5586497684)
- [#57历史总项进度索引](https://github.com/ywyz/kindergartenManager/issues/57#issuecomment-5586498129)
- [#55平台进度索引](https://github.com/ywyz/kindergartenManager/issues/55#issuecomment-5586498498)

没有改写Issue历史正文/证据、关闭Issue、创建PR或提交。所有本门源码路径仍是未提交本地文件。
下一轮提示词：[WP-B每日活动名称](../WP-B-prompt.md)。部分周成员规则仍待用户答复，WP-B名称切片不依赖该决定。

## 2026-09-09 用户补充确认

部分周任职允许整周读、编辑、导出，已同步ADR/spec/service契约。此前“待用户答复”为2026-09-08历史状态，现产品决策缺项解除。每日来源仍按精确日期授权，成员撤销与源写权限不变。
剩余总门缺项：完整新共享业务RED、真实宋体/目标Office单页证据（长样本及六列compact仍溢页）。正式模板资格属于后续WP-E，不能作为WP-A已完成证据，也不在本轮提前实施。

R4已获reviewer独立窄复核关闭；当前finding H/M/L=1/0/0。补充决定已[回写#77](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5592555046)。

## 2026-09-09 用户批准阶段调整

依赖新共享生产接口的可执行业务RED移至WP-C首道门，须在对应行为GREEN前完成。WP-A保留冻结契约、场景与断言及现有差距RED；没有把未执行测试改记通过。WP-D/E专属行为仍在各自实现前验证。
R1原发现保留历史事实，按获批阶段调整移交WP-C，不再作为WP-A阻塞；文档一致性待独立复核。真实宋体/目标Office五/六列单页证据仍缺，WP-A总门继续BLOCKED。本次只有文档调整，无产品实现或数据库操作。

独立reviewer窄复核确认阶段移交一致：R1按用户批准移交WP-C，阶段调整复核CLOSED；未执行测试仍未执行，各门先RED再GREEN要求保留。此结论不改写历史finding计数，也不等于产品Review 0/0或WP-A通过。WP-A剩余阻塞为目标字体/Office单页证据。

阶段调整已[回写#77](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5592757010)，并同步#75的WP-C依赖门。

## 2026-09-09宋体安装与重新验证补充

用户授权的三处字体安装及本机真实SimSun试排已完成，详见[新账本](WP-A-fonts-20260909.md)。旧/tmp二进制已失，本次为新样本/新hash而非旧hash复跑；short/compact各1页、long各2页，Windows Word仍缺。历史“缺真实宋体”已被本轮限定环境证据替代，不能外推所有服务账号/容器。WP-A仍BLOCKED。
