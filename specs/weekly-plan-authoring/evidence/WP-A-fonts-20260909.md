# WP-A 宋体安装与重新验证

2026-09-09，用户提供 `/home/ywyz/下载/Fonts.zip` 并明确授权在本机、`ssh aliyun`、`ssh bwh` 安装字体及重新验收。
仅字体安装与隔离合成排版检查；不部署应用、不重启业务、不操作数据库、不修改月计划或正式模板/profile。
工作SHA仍为 `8dcc83376577695b562529ec42adc6b48817f004`，本地文档未提交。

## 输入与安装边界

字体包 SHA-256 `223bbf601ba16dfdf96ce93f331b54753e4a9a52c1aaebd8337daa4f48743a6f`。
仅提取与本任务相关的 `Fonts/simsun.ttc` 和 `Fonts/simsunb.ttf`，没有安装整包其它字体或执行包内程序。

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| simsun.ttc | 18316748 | 1526ac24375f51f6eb73bc2d3f8072dbe4a80a3a65217677c9d9a84f67dab2ab |
| simsunb.ttf | 21894056 | 2a476ca00b5fbbbc12a4c5335634722b11a495ed87429284325701f1e36b596a |

本机没有免密sudo，因此安装到当前用户 `/home/ywyz/.local/share/fonts/km-simsun-20260909/`，权限0644。
`fc-cache -f` 后 `fc-match 宋体` / `fc-match SimSun` 均准确选择上述 simsun.ttc，字体家族 SimSun/宋体。
TTC含SimSun和NSimSun；fontconfig fontversion=343409。用户级安装不能推定其它服务账号或容器可见。
两台远端由独立子代理负责，均限制在各自字体目录和专属报告，不接触另一主机/仓库/业务数据，不递归委派。

## 证据连续性

上一轮 `/tmp/km-wpa-layout-20260908` 已不存在，不能声称原DOCX hash精确复跑。
仓库 `layout-artifact.md` / `layout-manifest.json` 保留历史描述与hash，但旧二进制证据当前不可访问。
本轮依据同一未修改仓库模板与已冻结结构重建样本，作为**新样本、新hash的重新验证**，不是旧结果替换或历史PASS。
新字体、脚本、样本、PDF、PNG、报告保存在持久目录 `/home/ywyz/code/km-wpa-fonts-20260909/`，字体二进制不加入仓库。

## 当前验证范围

使用Documents技能与bundled LibreOffice进行真实渲染，逐页检查实际字体、页数、裁切与合并。
本机未找到winword/WINWORD.EXE/wine/powershell.exe，无已验证可用Windows Word环境；Linux通过不能代替Word门。
宿主字体安装与生产容器字体可见性是不同状态，本轮不修改镜像或容器、不推定生产exporter已具备字体。

本报告在各安装与排版子任务收敛后追加实测结果；安装完成不等于WP-A单页门通过。

## 阿里云安装实测

已在宿主系统目录 `/usr/local/share/fonts/km-simsun-20260909` 安装，root:root、目录0755/文件0644。fontconfig原缺失，仅新增该包，0升级。传输前后与安装后hash一致，宋体/SimSun解析到目标TTC。无LibreOffice，未进行远端DOCX渲染。详见[脱敏报告](font-aliyun-20260909.md)。

## BWH安装实测

已安装到 `/usr/local/share/fonts/km-simsun-20260909`，目录root:staff 2755（继承父目录setgid），文件root:staff 0644。没有覆盖已有目录，传输及安装hash一致，fontconfig2.15.0识别宋体/SimSun，上传暂存已清理。无LibreOffice，未进行远端DOCX渲染。详见[报告](font-bwh-20260909.md)。三处字体安装与解析均完成，尚不意味着排版验收完成。

## 安装记录独立复核

reviewer只读复核三处安装报告及本账本，未发现阻止性问题，确认没有把fc-match当排版通过。公开回写仅用脱敏摘要，不复制远端主机标识。

Main已先核对新前置周日六列short：pdffonts仅列嵌入SimSun，pdfinfo为A4、1页，截图全部列和底行在页内。完整样本矩阵及布局复核仍以随后收敛的manifest为准。

## 本轮新样本页数与字体

Main使用pdfinfo/pdffonts逐文件核对9份PDF：全部嵌入SimSun，未列Noto。

| 内容 | 正常五天 | 前置周日六天 | 周六六天 |
|---|---:|---:|---:|
| 最短完整 | 1页 | 1页 | 1页 |
| 长中文 | 2页 | 2页 | 2页 |
| 缩减候选 | 1页 | 1页 | 1页 |

候选仅验证保留固定数量的合成排版能力，不代表用户正式采用、产品AI缩减流程或正式exporter验收。旧Noto样本二进制已失，且本轮是重建样本，不能将页数差异完全归因为字体或当严格A/B实验。完整新样本内容/hash和最终可视审查见新manifest。

## 隔离渲染字体可见性

Documents renderer会覆盖HOME；无额外配置的对照解析仍为Noto。本轮以 `FONTCONFIG_FILE=/home/ywyz/code/km-wpa-fonts-20260909/layout/fontconfig/fonts.conf` 显式包含已安装字体目录，并使用独立缓存。实际9份PDF全嵌入SimSun；不是只改DOCX字体名或fc-match结果。
复现入口：`sh /home/ywyz/code/km-wpa-fonts-20260909/layout/reproduce.sh`（会重建本轮专属目录内样本及证据；先保留既有manifest，重建后的文件hash另记）。

## 最终独立复核与状态

reviewer独立核对9 DOCX+9 PDF+12 PNG共30项hash全部匹配；A4、正文宋体12pt/标题16pt、exact20pt、9行及5/6列grid10466和合并边框符合候选契约。逐PDF仅SimSun，页数矩阵一致；代表短/compact与长文首尾页未见裁切或叠压。未新增H/M finding，仅关闭本轮样本几何/证据诚信复核，不宣称产品Review 0/0。

最终manifest SHA-256 `b395c5930f2e53c5f148aab76d4bdbe2c3b589e9fb3cb085927013ee7a017d2c`；artifact `d7458782e31dea9bc26021c895faf63c42e5a66584ba0411894e322c4a24ce10`。

三处字体安装完成；本机指定bundled Linux renderer的SimSun短/compact单页诊断通过。长样本仍2页，候选未作为真实业务内容采用；Windows Word实际渲染/独立检查仍缺，且Linux客户端为记录的alpha版本，不能外推其它Office。WP-A总门仍BLOCKED，不能将安装、单客户端或试排当正式资格/导出通过。

最终冻结清单补充复核：b395c593…版本仅补充验证脚本与元数据，30项样本hash引用独立重算0 mismatch；副本一致，无新finding。Main执行verify_layout.py得到VERIFY_OK，原工作区输入hash仍全部一致，产品/templates/alembic无差异，diff检查通过。Issue回写网络响应待确认，不将请求发起当发布成功。

## Issue回写完成

本机gh回写遇代理503/认证错误，已停止悬挂请求，通过现有GitHub连接器先确认无重复评论，再发布并读回最终hash。
- [#77完整字体与排版补充](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5604570090)
- [#56模板资格依赖更新](https://github.com/ywyz/kindergartenManager/issues/56#issuecomment-5604578508)

当前没有Issue回写阻塞。未关闭Issue、创建PR或提交；Windows Word缺项仍保留。
