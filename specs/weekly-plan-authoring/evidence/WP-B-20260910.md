# WP-B 每日活动名称：本地最小 GREEN

2026-09-10。WP-B授权的本地功能实现、专项验证和独立复审已完成；不是完整Quality/CI、云端、Office、Release或部署PASS。
WP-A继续BLOCKED，WP-C～WP-F未实施。没有关闭#77/#57/#75，没有PR、push、发布、生产部署或真实业务库迁移。

## 实时基线与保全

- 实时读取#77全部当前评论，包含WP-A首次回写、部分周补充、用户批准RED移交及2026-09-09宋体再验收。
  R1已按批准移至WP-C首门；R4已解除。当前WP-A缺Windows Word/目标Office，不再误称本机缺宋体。
- 原工作区HEAD `01bf6349a7c6f5268eba4425212f546874d651b0`；WP-A worktree实际HEAD
  `8dcc83376577695b562529ec42adc6b48817f004`，存在暂存+未暂存设计/证据。
- 本轮fetch后origin/main仍为`8dcc83376577695b562529ec42adc6b48817f004`，包含bd2457a游戏保全。
  源码唯一Alembic head为`3c9f4b2a7d1e`。没有连接业务库探测schema。
- 新worktree `/home/ywyz/code/km-wpb-20260910`，分支`feat/wp-b-activity-name-20260910`；
  WP-A工作文件逐一复制/hash核对（不复制Graphify cache、业务配置或数据库）。原两处工作区最终HEAD、
  git status、暂存diff hash及所有未提交输入文件hash一致，保全manifest保存在专属证据目录。
- 本地工作提交（尚未push，无可访问GitHub提交链接）：
  **`41b63c9cf6492d31c91faa07bd021e4096eb5379`**（前置实现提交`ab4c22b1f6e9d0a9489a65a10fab577bfce60f4f`）。
  本轮两个提交累计只包含本门21个代码/测试文件；WP-A复制设计及本次交付文档仍未提交。
  各组测试后记录对应同内容提交；最终commit后app/alembic/tests/Agent tests/templates无未提交差异。
  没有远端分支包含该提交，没有本门CI运行或可声称成功的headSha。

## 实现文件（均为上述worktree本地路径）

- `app/core/activity_name.py`：精确文本/256 UTF-8 bytes边界，不截断或强制类型转换。
- `app/core/models/daily_plan.py`、`app/repository/daily_plan_repository.py`：独立可空名称、保存白名单；
  旧NULL不回填，保持tenant/user及plan ID/revision CAS，名称单独变化+1/no-op不变。
- `app/integration/ai_client/lesson_plan_client.py`、`app/service/lesson_plan_service.py`：
  默认六字段；旧自定义五字段兼容空名称且服务提示手填，不改存储prompt/active/version。
  明确标注标题、书名号标题及教学字段前的独立标题才允许保留；来源无法证明时为空并提示手填，
  不以目标/过程/反思或单独正文句子编造名称。此为保守识别，不宣称任意排版的标题都能自动识别。
- `app/ui/pages/daily_plan.py`、`app/ui/pages/prompt_mgmt.py`：输入、split回填/提示、冻结payload、
  手改generation、清空/删除/选日期/重载及prompt测试显示。每日导出继续读取已保存、版本匹配的行。
- `app/api/schemas.py`：显式名称；原只读API tenant投影保留，未新增共享API。
- `app/integration/word_export/exporter.py`：teacherplan已有R6标签为“活动主题：”，原标签/行结构保留，
  只填名称；名称空白/换行保持，其他字段仍用既有trim。既有fallback也携带名称，不冒称正式模板资格。
  teacherplan/weekplan/monthplan源码二进制及周hash/profile、正式周exporter未改。
- `app/service/agent/confirmed_write.py`：新完整操作前snapshot显式`snapshot_schema_version=2`，含名称；
  无标记旧JSON严格解释为v1，JSON/hash/audit不重写，reconcile保持版本/hash/actor/路径严格校验。
- `app/service/agent/confirmed_plan_read.py`：仅本地确认后重载投影携带名称，末尾默认空值保持旧构造兼容。
  不是Provider READ输出扩张。名称不在Patch/DRAFT写集合，Provider仍恰好4 READ+2 DRAFT。
- `alembic/versions/6a8d2c4e9f10_add_daily_activity_name.py`：从实时旧head派生；原迁移源码未改。
- 新测试：`tests/test_activity_name.py`、`tests/test_activity_name_ui.py`、`tests/test_activity_name_migration.py`、
  `specs/agent-write/tests/test_wpb_name_snapshot.py`。
  更新既有client/service/API/session守卫与新snapshot期待共5个测试文件；完整文件/hash见manifest。

## RED → GREEN

| 阶段 | 实测 | 边界 |
|---|---|---|
| 名称新专项+WP-A两个名称探针，连续两次 | 各15 failed / 4 passed / 14 deselected | 明确名称被过滤、缺字段、类型上界未校验、存储/API缺名称等；4通过是原拒绝未知字段，非新正向行为 |
| SQLite迁移专项，连续两次 | 各2 failed | 真实升级后的表仍无名称，不是missing import |
| Agent专项，连续两次 | 各1 failed / 1 passed | 名称未持久化/未让旧确认过期；旧v1对账基线通过 |
| 页面处理函数补充 | 基线源码双跑各2 failed，当前2 passed | 使用git show出的原生产handler，widget doubles+真实repository；这是实现后的基线复核，非伪称提前运行 |
| 初始名称/迁移GREEN | 21 passed（含WP-A两项），Agent2 passed | 隔离SQLite、mock AI |
| 复审标题边界 | 5 failed / 3 passed → 全通过 | 书名号保留、正文不得冒充名称 |
| 复审Word保真 | 1 failed → 通过 | 名称两端空白/换行不得strip |
| 空白标题 | 1 failed → 1 passed | 空标注+空白AI名称回空并提示 |
| 最终范围复核课程审议示例 | 1 failed → 受影响137 passed | 仅split输出示例新增名称，课程审议按其原解析器输出契约恢复 |

原WP-A未来共享/日期/整周结构差距仍保留，没有为了全绿放宽旧周/月契约或撤销bd2457a。
末次Main发现prompt管理的课程审议示例被误加名称，先以真实解析器_REQUIRED_KEYS建立RED，
只撤回该字段；最终窄review结果追加至Review账本。
详细Reviewer结果见[独立复审](WP-B-review-20260910.md)：最终开放H/M/L=0/0/0，独立209 passed。

## 最终验证

下表完整回归绑定前置实现内容`ab4c22b…`；之后仅撤回误改的课程审议输出示例并新增守卫测试，
最终`41b63c9…`执行受影响集合**137 passed**。没有把前置SHA全量测试冒称最终SHA重新全跑。

全部在Python3.14.7、合成数据、mock AI、专属临时设置目录运行，无真实模型请求。
完整日志/hash见`/home/ywyz/code/km-wpb-evidence-20260910/`及[manifest](WP-B-manifest-20260910.json)。

| 检查 | 结果 |
|---|---|
| 常规`tests/`全量 | **1199 passed / 1 skipped**；跳过另案显式启用R5_MYSQL_LIVE的MySQL备份恢复演练 |
| 名称+API/页面/提示词/每日/Agent WRITE集合 | **512 passed / 1 deselected**；明确排除已独立证明的Bootstrap基线失败 |
| `specs/agent-foundation/tests` | **261 passed**；Provider/Tool零持久化/能力边界回归 |
| 旧周/月specs+`test_released_weekly_plan_game_completeness.py` | **275 passed** |
| 新文件Ruff与format、diff whitespace | 通过 |
| 所有涉及旧Python文件的完整Ruff | **52条告警，未通过**；同文件在未改WP-A基线也是52条，诊断多重集差异0 |

完整Agent WRITE集合曾实跑Bootstrap文案断言失败：
`test_bootstrap_cli_migration_failure_has_nonzero_process_exit`期待“迁移失败”，当前受支持显式迁移提示为
“请先运行显式数据库迁移并检查连接状态”；在WP-A未改源码独立单跑同样失败。
本门不更改该无关业务或测试，不把deselected当作PASS。故不能宣称全仓库/完整Quality全GREEN。

验证纠错：首次把两个specs测试目录放在同一pytest进程导致既有`from conftest`重名collection冲突，
已拆进程；Foundation初次外部KINDERGARTEN_DATA_DIR覆盖其自身fixture根，出现37 setup errors。
修正为专属tmp内初始化settings后解除该env，由现有fixture控制路径，261项通过；不改产品为fixture凑绿。
这些失败日志保留，不与最终运行结果混用。

## 迁移实测

唯一新head **`6a8d2c4e9f10`**，SQLite **3.53.1**与专属临时MySQL **8.4.11**均实际升级。
MySQL镜像digest：`mysql@sha256:b3b90af2a6552ae30c266fdb7d5dd55f3afb72404bb78d37fe8a23eb857fd3fb`。
容器`km-wpb-mysql-20260910`只绑定本机回环临时端口、tmpfs数据、合成schema；最终独立空schema复验后已删除容器及tmpfs（2026-09-10）。
没有连接现有MySQL容器、远端或业务库。脚本/脱敏报告保存在专属证据目录。

- 从旧head种合成旧行，再upgrade：旧字段数据/索引保留，新名称NULL；没有批量补名称或重写旧版本/审计。
- 迁移用原生ADD/DROP COLUMN，不重建表；现有字段/约束/索引及initial trigger保留。
  revision step trigger包含名称，MySQL使用NULL-safe binary字节比较，大小写/尾空格变化分别+1。
- 实测拒绝跳revision、名称不递增revision、纯revision bump、超限名称与非法初始revision；
  SQLite另验证BLOB名称拒绝。业务repository另做精确类型校验；数据库文本列本身可能类型转换，不声称能识别SQL输入的Python类型。
- 非空名称普通downgrade在任何DDL前拒绝，拒绝后值/revision仍完整；空库单独downgrade/upgrade通过。
- 首次MySQL脚本trigger计数误含两条历史operation_version trigger而失败；修正检查限定daily_plan表，
  在第二个全新合成schema重新全流程通过。最终另起专属容器复验：保留非空拒绝后的schema及名称，
  在独立新建空schema做upgrade/downgrade/upgrade，完整PASS后才删除整个专属容器/tmpfs。
  日志mysql-final-independent.log；未通过清空名称来让非空库downgrade通过。未修改产品触发器去满足错误计数。

## WP-A剩余与下一步

WP-A仍缺Windows Word实际打开/渲染/页数/无裁切及目标稳定Office证据；9月9日本机真实SimSun结果保留，
short/compact一页、long两页，不外推正式模板资格或生产容器。新五/六列资格仍归WP-E/#56。
共享可执行业务RED按用户批准归WP-C首门，不能用WP-B名称通过代替。

下一步先做WP-C最小权威identity/member与共享生产seam的RED，按部分周交集授权、逐日源授权、
共享唯一/CAS、撤销、人工选源、来源/映射revision及日期占用唯一逐步推进；不顺带完成#75升班/历史范围。
可直接使用[WP-C下一门提示词](../WP-C-prompt.md)。本次没有执行WP-C。

## Issue回写

已发布并读回校验[#77本门回写](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5609841302)，#77/#57/#75仍OPEN。回写只列本地路径，未伪造可访问代码或CI链接。
