# WP-C 首道门推进：身份子步，本门未整体闭合

2026-09-10。完成第一身份子步的本地代码与隔离验证；**不是WP-C全部功能GREEN**。
共享唯一根/CAS、实际教学日授权、来源显式映射/人工选择/快照等仍缺真实seam，不能以本步通过代替。
WP-A仍缺Windows Word/目标稳定Office，WP-D/E/F均未提前实施。没有push、PR、关闭Issue、发布或部署。

## 实时输入与保全

读取#77/#75当前正文和全部回写、AGENTS.md、ADR-0011、weekly-plan-authoring的spec/tasks/
migration-proposal/service-contract/calendar-contract、WP-B-20260910.md及WP-B独立Review。
线上输入快照保存在 `/home/ywyz/code/km-wpc-evidence-20260910/issue-{77,75}.json`。

- 原工作区HEAD `01bf6349a7c6f5268eba4425212f546874d651b0`；原WP-A HEAD `8dcc83376577695b562529ec42adc6b48817f004`。
- WP-B HEAD `41b63c9cf6492d31c91faa07bd021e4096eb5379`，仍有未提交WP-A/WP-B文档。
- 两次fetch后的 `origin/main=8dcc83376577695b562529ec42adc6b48817f004`；远端无包含WP-B提交的分支，GitHub commit API返回422。
- WP-B源码Alembic head实时为`6a8d2c4e9f10`。没有连接业务数据库探测。
- 新工作区 `/home/ywyz/code/km-wpc-20260910`，分支`feat/wp-c-identity-20260910`，直接派生本地WP-B提交。
  WP-A/WP-B未提交文档逐文件复制/hash核对；不复制数据库、凭据或Graphify缓存。
- 最后逐项复核原工作区/WP-A/WP-B的HEAD、status、index diff hash、所有未提交文件hash完全一致。
  保全清单 `/home/ywyz/code/km-wpc-evidence-20260910/preserved-inputs.json`。

## 实际交付

本地代码提交 **`00afdc878b306475508c777997956cdf4638dbef`**，11个代码/测试文件。
GitHub commit API同样422；无可访问提交链接，无本门CI/headSha。所有路径均指上述本地worktree。
WP-A/WP-B复制资料及本门交付文档保持未提交，不挟带进入代码commit。

- `app/service/academic_identity/{application,contracts}.py`：真实application入口与关闭DTO。
  `create_year/create_semester/create_class/bind_class/grant/revoke`可调用；production composition读取当前浏览器token。
- `app/repository/academic_identity_repository.py`：真实tenant限定持久化、重叠拒绝、成员revision/CAS、事务成功审计。
- `app/core/models/academic_identity.py`：academic_year、semester、class_instance、class_semester、
  teacher_class_assignment、tenant_identity_manager，加配置守卫与identity_audit。
- `app/jobs/identity_manager.py`：独立、默认禁用的资格授予/撤回任务。隐藏输入签名token，精确确认target ID、
  expected_revision，显式运维enable和远端开关；actor取真实session，当前sys_admin只在此受控任务有该窄能力。
  不接页面/API，不自动种资格、不迁移。普通身份应用仍须独立manager行；管理资格不授正文权限。
- `alembic/versions/7c91e2a4b610_add_academic_identity.py`：派生实时WP-B head；User新增复合唯一index，
  同tenant/同学年FK，审计禁止UPDATE/DELETE，非空身份数据在DDL前拒绝downgrade。
  User ORM同步相同index；旧迁移、DailyPlan内容/revision、旧周/月政策不改。
- `tests/test_wpc_identity.py`和`tests/test_wpc_identity_migration.py`：实际签名token/resolver、实际service/repository、
  Alembic数据库；不用身份假服务或metadata.create_all作为本门证据。竞态仅加调度屏障，仍执行真实业务事务。

接口冻结见 `../WP-C-step-1-contract.md`。显示名只保存显示，不匹配、不合并。
一个学年内既存class可显式绑定第二学期；不同学年组合拒绝，不实施升班/cohort。
管理与资格撤销共用User升序锁，配置还锁tenant guard，任职锁class_semester后再assignment。
SQLite BEGIN IMMEDIATE；MySQL READ COMMITTED + SELECT FOR UPDATE。锁后重新decode token并比较DB auth_epoch，
旧jti/role/失效资格均拒绝。MySQL必须避免锁前一致性读取留下RR快照。

## RED → GREEN（按实际顺序，不回填历史SHA）

| 执行阶段 | 实际结果 | 证据/边界 |
|---|---|---|
| 初始Alembic schema断言双跑 | 各1 failed | WP-B升级库没有权威表；只算schema差距，非共享业务RED |
| 可调用身份seam上的业务双跑 | 各4 failed / 2 passed | year/semester重叠错误成功、重复assignment错误成功、双授权两成功 |
| 守卫锁内拒绝重叠后 | 7 passed | 当时业务6项+schema1项 |
| 审计闭合真实双RED | 各1 failed / 6 deselected | action为method字符串；改为identity_manage+闭合operation_kind/outcome/reason后通过 |
| MySQL全新schema双RED | 两次driver errno3822 | 多表CHECK名称重复；表名前缀修正 |
| MySQL后续DDL失败 | errno3818，重跑复现 | tenant guard主键被隐式自增；显式autoincrement=False |
| MySQL真实job失败 | errno3819，多新schema复现 | operation_id字符串被错误加正数CHECK；约束仅适用于整数ID |
| Main补反向撤销竞态双RED | 各1 failed / 11 deselected | MySQL RR锁前快照使等待操作在撤销提交后仍成功；事务首读前READ COMMITTED修正 |
| 最终本地专项/回归 | 见下表 | 精确绑定最终提交 |

初始可调用seam的正向管理与既有拒绝路径、后来新增bind/约束/回退等覆盖并非全部经历独立失败，
不把所有后补通过用例称为提前RED。RED轮当时是未提交工作树，未逐轮生成代码提交；日志保留，
不伪造当时SHA或远端链接。最终代码SHA与测试绑定如下。不存在shared/source接口的场景仍“未执行”。

环境失败另记：首轮测试设置目录的祖先group可写，改用`/home/ywyz/.km-wpc-test-20260910`（0700）；
首次MySQL迁移脚本误用同步驱动导致async driver错误，改为应用支持的aiomysql URL再真实升级；
补User index时机械替换误入mapped_column导致collection TypeError，立即仅保留预期两行并复跑。
以上不计业务RED，也不归咎于WP-B既有失败。迁移失败阶段的fixture连接退出告警不混入最终结果。

## 最终SHA验证

Python3.14.7；合成用户/数据，无真实AI或业务库。日志根：`/home/ywyz/code/km-wpc-evidence-20260910/`。

| 集合 | 结果 | 日志 |
|---|---|---|
| 身份/迁移15项+User/session/WP-B名称/每日兼容/旧周游戏专项 | **86 passed** | final-sha-regression.log |
| 旧周/月specs | **272 passed** | final-sha-legacy.log |
| 专属MySQL真实身份业务（含并发/双向资格撤销竞争） | **12 passed** | final-sha-mysql.log |
| reviewer独立SQLite身份/迁移 | **15 passed（末次纯标注/index同步/CLI异常捕获前）** | 最终commit另作只读窄审；不伪称独立重跑最终SHA |
| 新Python文件Ruff/format，git diff whitespace | 通过 | 仅本步新增文件；不声称全库Quality |

没有全仓、CI、CodeQL、浏览器、云端、Word或LibreOffice本门验收。
WP-B既存Bootstrap断言/Ruff告警原样另案，未隐藏本门新增失败，也未扩散重构。

## 精确迁移环境

SQLite由实际Alembic升级到`7c91e2a4b610`，每例独立tmp数据库、连接foreign_keys=ON。
验证旧daily行/索引完全保留、无自动映射/assignment、跨tenant FK与跨学年拼接拒绝、非空回退在DDL前拒绝，
拒绝后原行和身份数据仍在；独立空库upgrade/downgrade到WP-B head/upgrade成功。

MySQL **8.4.11**，专属容器`km-wpc-mysql-20260910`，镜像
`mysql@sha256:b3b90af2a6552ae30c266fdb7d5dd55f3afb72404bb78d37fe8a23eb857fd3fb`，
仅127.0.0.1:32770映射3306，tmpfs数据、专属合成schema；没有连接现有/远端/生产MySQL。
容器仅用于一次性测试，空root credential不复制任何真实凭据。所有schema在销毁专属容器时一并清理。

迁移复现脚本 `/home/ywyz/code/km-wpc-evidence-20260910/mysql_migration.py`；报告`mysql-migration.json`。
4次复合FK非法写拒绝、旧daily正文/revision/索引保留、非空拒绝后的数据/schema/revision保留，
另建独立空schema往返全部通过；没有清空非空schema来制造downgrade成功。
reviewer只读Main MySQL报告，未声称独立二次MySQL运行。

## 逐项尚缺：不得勾选WP-C首门全部通过

1. **共享政策/实际教学日输入seam尚缺**：新shared_weekly_v1专用分派、当前assignment部分周至少一天交集、
   零交集拒绝、assignment自身撤销/过期/跨class/semester及旧页面、整周read/edit/export权限均未执行新业务RED。
   本步测的是身份管理与管理资格撤销，不能代替共享成员访问授权或最终下载授权。
2. **共享根/版本/repository尚缺**：唯一root、双教师创建收敛、同version CAS一成一败、不可变版本、
   无正文共享审计、日期占用唯一尚未执行。不能以assignment并发测试代替shared CAS。
3. **来源/映射seam尚缺**：daily显式映射预览/管理员确认、mapping ID/revision、逐日源授权、本人+他人重复人工选择、
   来源变化只提醒、重导入差异采用、日期占用/源revision漂移等未执行；旧未映射daily继续原个人路径，不自动跨教师共享。
4. 正式周exporter、日历规范化/固定结构/AI、填写页/模板资格/Office/云端保留WP-D/E/F。

下一提示词：`../WP-C-next-prompt.md`。先做一个共享授权小步，不一次展开整个WP-C；取得真实RED再修。
不能把身份子步的review 0/0/0当成WP-A、WP-C全门或#75完整验收通过。

专属MySQL容器及tmpfs已在最终验证后删除（2026-09-10）；包括所有失败/成功测试的专属schema。
未改任何其它容器或数据库。原工作区/WP-A/WP-B保全再次核对通过。

运行时实测Python3.14.7 / SQLite3.53.1。
已回写并逐字读回核对[#77评论](https://github.com/ywyz/kindergartenManager/issues/77#issuecomment-5610075688)，Issue仍OPEN。
日志只保留结果与失败摘要，去除SQL/token/fixture展示；本地证据hash清单为
`/home/ywyz/code/km-wpc-evidence-20260910/manifest.json`。最终app/alembic/tests相对代码SHA无未提交差异。
