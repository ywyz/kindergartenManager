# WP-E 合成长文提案逐组确认（2026-09-14）

本记录是用户内容确认记录，不是页面采用、保存、渲染或 Word 验收证据。

用户在阅读第1组“本周重点，共3条”的前后对照后回复“接受，请同步到仓库”，随后明确授权“同时推送当前交付分支，不创建 PR 或合并”。本次只确认已展示的三个字段，不将其扩大为全部26字段已接受。

| 字段 | 原文精确定义 | 已接受提案精确定义 |
|---|---|---|
| `focus.0` | `围绕主题进行观察，`连续10次，直接接`20` | `围绕主题观察，`连续10次，直接接`20` |
| `focus.1` | `围绕主题进行观察，`连续10次，直接接`21` | `围绕主题观察，`连续10次，直接接`21` |
| `focus.2` | `围绕主题进行观察，`连续10次，直接接`22` | `围绕主题观察，`连续10次，直接接`22` |

这里的“连续10次”是展示记法，实际内容保留10次重复，不将10次压缩成1次。三条数量与数字后缀保持。其余23个候选字段尚未确认；七个名称、每日字段、日期、人员、主题和事实不改。

展示前只读核验合成plan6仍为current_version12/revision2，body SHA256为`0db47facf507bf060db4aa2587384fb91e7c0bd9c61a7e07d12e859316718ec4`，与上一轮证据一致。本次没有调用页面采用、拒绝、保存、重检或下载，也没有修改合成数据库、模板和十二历史候选。页面候选包含全部26字段，因此不能以这3条确认触发整份采用；其余字段需继续逐组展示确认。

后续全部内容确认后，仍须从真实页面核验候选/版本及TTL，通过正常入口执行显式采用→仅页面变化核验→显式保存→真实重检；超页则拒绝文件。不得重启/重建状态绕过两轮计数，出现实质新差异需重新确认。旧候选不会因本次内容确认而被视作仍有效。

仓库同步范围为`codex/wp-e-startup-handoff-20260913`的普通推送，不创建PR、不合并、不部署、不关闭Issue。推送是否成功以实际远端ref核对和最终执行回执为准，不由授权语句推定成功。

本次仅文档变更，tested_code_sha仍为`2c310b7f170c8cc509e0c9ddcb8d8c0a2322c427`。旧文档后继`5235998299286b3f443ca067bee9e6c7242e9b3e`、旧ZIP/bundle/manifest作为2026-09-13封存时点保留，不覆盖重写；本记录由仓库新增提交交付。历史“全部待答复/未push”文字仅代表当时状态。

阶段二/三、当前Word原生复验和正式资格仍NOT_RUN，WP-F未执行。未把用户接受文本记为任何验收PASS。
