# WP-E Windows 原生 Word 专项验收提示词

范围校正：2026-09-13 用户明确当前只验收 Windows，目的是解除 WP 系列开发的 Windows 验收阻塞。本提示词取代本轮执行中的 Windows/Ubuntu 联合安排；联合提示词保留为后续参考，不按其中 Ubuntu 测试站任务开展本轮工作。

---

继续 KindergartenManager WP-E，当前仅完成 Windows 原生 Microsoft Word 专项验收。Windows 指 DOCX 兼容验收客户端，不是新增 Windows 桌面安装版产品。不要要求先完成 Ubuntu 测试站、云端部署、真实模型或产品数据库保存链才能补验 Windows；这些门留在 WP-E 后续账本，不误记为已经完成。

## 1. 核对当前材料

保留 main、未提交文件、当前隔离 worktree 和历史原件。读取实际分支 AGENTS.md、ADR-0011、specs/weekly-plan-authoring/spec.md §2.2–2.7、tasks.md WP-E/F、WP-E-local-contract.md，以及 evidence/WP-E-native-Word-20260912.md、evidence/WP-E-dedup-recheck-20260912.md。按现行文件及最新用户范围执行，不使用旧 WMP-9 基线/归档 DOCX 替代。

当前产品 SHA 已知为 `0bded3377c01956833bcbb6cd306f9a7f5d924e1`，文档后继已知为 `247fc3472481bcdc7b161b36301e08f205da9c71`，现场核实工作树与产品差异，不假定远端已含本地提交。运行 validation/wp-e-handoff-20260912/verify_bundle.py；预期 `INTEGRITY_OK: 26 files; not Word or WP-E PASS`，失败不改清单。

外部材料根目录：`C:/Users/admin/.codex/visualizations/2026/09/12/01a0937c-3f64-7ce2-98f9-fecd1934901c`。

- `wp-e-header-week-20260912/candidate-manifest-final.json` 和 `candidates-final`：八份当前 renderer 原始候选。
- `wp-e-native-final-20260912`：原生环境、12页观察、49项材料清单。
- `wp-e-reduction-20260912`：四份去重提案、用户采用记录、accepted-files、新8页观察及38项清单。

先核对适用材料 hash 和 renderer/seed 绑定，保留清单原始范围；26份材料不包含新 DOCX/截图，也不代表完整外部证据包。

## 2. 本轮优先完成正常五／六列格式门

已有四份正常样本（five-normal、five-holiday、six-sunday-normal、six-saturday-normal）均实测 A4 一页，但尚不是完整格式 PASS。优先逐件补验这些可容纳输入，不因长文两页停下所有 Windows 工作。

1. 使用原生 Word。Windows Server 2025 Datacenter 已获用户接受，不因 Server 名称或旧 WMP-9 通道政策阻塞。记录实际 OS 与 Word 账户/关于的具体产品、版本和 build；必要记录不包含邮箱等无关个人信息。前次环境记录可作核对线索，不伪称新现场观察。
2. 原生确认正文宋体12pt、固定20pt，标题按当前模板要求检查。先定位五列正常样本“整体行距混合”的具体段落：区分正文、空段落及合并格结构，不先认定缺陷，也不直接忽略。逐文件记录实际检查范围，不从一份外推全部。
3. 核对主题、班级后周次与日期、左上空白、星期下无日期、多姓名、空格、书名号、固定栏目/游戏/目标/总结数量、日期及假期空活动、换行与无样例残留。
4. 观察全部页，同时提供足够倍率的文字及上下/左右边界细节，确认实际一页、无裁切、重叠、缺字和字体漂移。50% 全页截图只证明总览，不能替代全部细查。
5. 输入/输出/证据逐件绑定 hash、代码 SHA 和客户端版本。原件不覆盖，未实测格子保持 NOT_RUN/BLOCKED。COM、OOXML、LO、PDF页数和浏览器布局不能替代原生 Word。原生控制不可用时，明确给用户必要操作清单，由用户提供全部页与属性观察材料。

## 3. 长文单独验收，不改写失败

四份原始长文各2页，用户确认的四份去重版仍各2页，单页 FAIL 保留。历史 long 三份两页及 compact 合成候选状态同样保留。

当前规范允许无法收敛并提示手改，不要求任意长文强行单页。因此分别记录：

- 可容纳内容的五／六列完整 Windows 格式结果。
- 原始及去重长文的真实超页负例，不能改成单页 PASS。
- 若为满足当前门仍需长文修订后单页成功例，另准备保留事实、固定条数、日期、名称、人员与格式要求的手改差异。展示具体差异后获得明确采用，再保存独立新文件、重新原生检查；不要无限重试，也不要暗中减字号/行距或删除必需条目。

已有去重的用户确认仅绑定该提案；同一提案不重复索取确认。后续新改写不冒充已经获采用。记录本地文件保存事实，不虚构产品数据库版本或真实模型调用。产品“无法收敛时正确拒绝下载”的应用行为留待后续页面验收，本轮截图不能证明该行为。

## 4. 只有确认 Windows 相关缺陷才修代码

证据缺项先补验。长文两页本身不能直接证明 renderer 有缺陷。若原生定位发现实际正文行距、字体、表格、裁切或内容填充违反当前契约，再按要求保全修复前源/hash、连续两次稳定业务 RED、最小修复、相关回归和独立只读复审；环境/测试错误不计 RED。

修复可在当前授权的隔离开发环境进行，不必等待 Ubuntu 测试站部署。若确需交给 Ubuntu 开发端，交付可复现问题、输入/证据 hash、期待结果和测试要求；不以“可能需要修改”代替定位。代码变化后重新冻结产品 SHA，重建并复验受影响的 Word 输入，旧 SHA 人工证据不自动继承。

不为本轮 Windows 补验顺带扩展自动缩减规则、产品 Agent、迁移或部署。若去重等能力扩展确有需要，单列后续开发事项。

## 5. Windows 专项交付与后续开发衔接

逐项交付 Windows 矩阵、原始文件与独立观察证据、hash 清单及独立只读复审。明确列出正常格式、长文负例、修订成功例、未实测项各自状态。只有本轮约定的 Windows 必需项实际满足才宣称相应 Windows 专项通过；若只完成候选格式，使用“当前 SHA/客户端下的候选格式验收”表述，不偷换成正式 released 模板资格。

若正式资格仍缺当前 released/catalog 事实，单列为后续资格绑定门，不因此停止已有材料的原生补验，也不伪造正式资格。Windows 可完成事项与依赖外部状态的事项分开交付，使后续 WP-E 开发能准确承接。

Ubuntu 页面全分支、真实模型、数据库保存、正式 catalog 安装、exact-SHA CI、OCI、迁移/部署不在本轮执行范围，保留为后续 WP-E 门。WP-C 四类历史 native 双 RED 仍 UNMET，用户允许继续 WP-E 本地工作的决定不变。Windows 专项通过不等于 WP-E 总门关闭；WP-E 未完整关闭不执行 WP-F、不提前写 WP-F-next-prompt.md。不推送、发布、操作生产或发送 Issue 消息。
