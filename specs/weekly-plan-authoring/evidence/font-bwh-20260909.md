# BWH 宋体安装与字体解析报告

- 验收日期：2026-09-09
- 范围：SSH 别名 `bwh` 主机；未操作 aliyun、本机字体目录、仓库、容器、数据库或秘密文件。
- 连接：`ssh -o BatchMode=yes` 成功，无交互认证；远端为 Linux x86_64（内核 `6.8.0-139-generic`）。SSH 身份为 root；`/usr/local/share/fonts` 原有目录可写。

## 输入文件

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `simsun.ttc` | 18,316,748 | `1526ac24375f51f6eb73bc2d3f8072dbe4a80a3a65217677c9d9a84f67dab2ab` |
| `simsunb.ttf` | 21,894,056 | `2a476ca00b5fbbbc12a4c5335634722b11a495ed87429284325701f1e36b596a` |

上传到远端专属临时目录后逐文件核对 SHA-256，再安装。安装前目标目录不存在；安装使用同一文件系统的暂存目录并原子改名，没有覆盖既有同名目录或文件。上传暂存文件已在验收后清理。

## 安装结果

- 目录：`/usr/local/share/fonts/km-simsun-20260909`
- 目录权限：`drwxr-sr-x root:staff`
- 文件权限：`0644 root:staff`
- 两个安装文件的 SHA-256 与输入文件逐一相同。

远端已执行 `fc-cache -f /usr/local/share/fonts/km-simsun-20260909`。字体工具存在于 `/usr/bin`，fontconfig 版本为 `2.15.0`。

只检查了常见 renderer 可执行文件的存在性：`soffice` 和 `libreoffice` 在该 SSH 主机上均不存在；没有启动任何 renderer 或业务进程。

## 解析验收

`fc-match` 结果如下：

```text
宋体  -> SimSun,宋体 | Regular,常规 | /usr/local/share/fonts/km-simsun-20260909/simsun.ttc
SimSun -> SimSun,宋体 | Regular,常规 | /usr/local/share/fonts/km-simsun-20260909/simsun.ttc
```

`fc-scan` 识别的家族包括 `SimSun,宋体`、`NSimSun,新宋体` 和 `SimSun-ExtB`。

## 边界

本报告只证明 BWH 宿主机已安装字体且 fontconfig 能解析它。若 renderer 在容器内运行，字体还必须存在于该容器的字体目录和缓存中；宿主机可见不等于容器可见。Windows Word 仍需在实际 Windows 验收环境单独确认。未启动 renderer 或业务应用，也未据此宣称页数、裁切、双平台或 WP-A 通过。
